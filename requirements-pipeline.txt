# PDF extraction
marker-pdf
pymupdf4llm
pdfplumber

# Embeddings / entity resolution
sentence-transformers>=2.7.0
scikit-learn>=1.4.0
numpy>=1.26.0

# Utilities (already in venv but pinned here for clarity)
tqdm>=4.66.0
python-dotenv>=1.0.0
neo4j>=5.20.0

# LLM — served locally via Ollama (no API key needed)
# requests is stdlib-adjacent; already available via streamlit deps
